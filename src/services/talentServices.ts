// Services liés aux talents (Profil, mises à jour, etc.)
import axiosInstance from '@/lib/axios';

export const getTalentProfile = async () => {
  // Exemple
  // return axiosInstance.get('/talent/profile');
};

